import { ValidnaFormaDirective } from './validna-forma.directive';

describe('ValidnaFormaDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidnaFormaDirective();
    expect(directive).toBeTruthy();
  });
});
